name: manter servico acordado

on:
  schedule:
    - cron: '0 0 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: enviar ping
        run: curl -I https://czeodzjyqjucmhuklmpr.supabase.co/rest/v1/